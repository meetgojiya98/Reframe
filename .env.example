# Database (Vercel Postgres, Neon, or any Postgres — use POSTGRES_URL or DATABASE_URL)
POSTGRES_URL=
# or: DATABASE_URL=

# NextAuth (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# OpenAI (for Coach)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# App
NEXT_PUBLIC_APP_NAME=Reframe
NEXT_PUBLIC_APP_TAGLINE=Reframe — shift your thoughts, gently.
RATE_LIMIT_RPM=20
BOT_PROTECTION_ENABLED=false
BOT_PROTECTION_TOKEN=
