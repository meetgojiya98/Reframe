# Database (required for signup/login)
# Supabase example: replace YOUR_DATABASE_PASSWORD with your Supabase project database password.
POSTGRES_URL=postgresql://postgres:YOUR_DATABASE_PASSWORD@db.cuilugmthdbkpvkwhyvw.supabase.co:5432/postgres
# or: DATABASE_URL=

# NextAuth (required in production; strongly recommended in dev)
# Generate secret with: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# OpenAI (Coach + app AI: recap, suggestions, skills, affirmation)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# AI enterprise (optional)
# AI_REQUEST_TIMEOUT_MS=25000
# AI_MAX_RETRIES=2
# AI_RPM_PER_USER=30
# AI_AUDIT_ENABLED=false
# AI_FEATURE_COACH=true
# AI_FEATURE_WEEKLY_RECAP=true
# AI_FEATURE_TODAY_SUGGESTIONS=true
# AI_FEATURE_SKILLS_RECOMMEND=true
# AI_FEATURE_AFFIRMATION=true

# App
NEXT_PUBLIC_APP_NAME=Reframe
NEXT_PUBLIC_APP_TAGLINE=Reframe — shift your thoughts, gently.
RATE_LIMIT_RPM=20
BOT_PROTECTION_ENABLED=false
BOT_PROTECTION_TOKEN=

# Optional: distributed rate limiting (Upstash Redis). When set, coach and other rate-limited routes use Redis instead of in-memory.
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
